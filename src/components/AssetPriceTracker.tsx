import React from "react";

// Empty placeholder to prevent build errors
// This component has been intentionally disabled as part of removing market price functionality
export const AssetPriceTracker: React.FC = () => {
  return null;
};
